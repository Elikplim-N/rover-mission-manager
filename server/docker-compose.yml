version: '3.8'

services:
  rover-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rover-api
    restart: unless-stopped
    environment:
      PORT: 3001
      DATABASE_URL: ${DATABASE_URL:-postgresql://rovadmin:S7ISb9PGLYnWzbB43fnu@178.105.184.157:6000/rover-hub}
    ports:
      - "3001:3001"
