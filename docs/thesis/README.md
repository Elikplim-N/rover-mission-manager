# Thesis Document

`DESIGN_AND_GHANA_Final.docx` is the project report, with the following figures added in this pass (source PNGs in `figures/`):

- Figure 3.1: system block diagram, generated from the verified module architecture.
- Figure 3.3: circuit wiring diagram, generated from the pin table and cross-checked against `firmware/MaizeRover_Phase2_Master/MaizeRover_Phase2_Master.ino`.
- Figures 3.4-3.7: real screenshots of the companion web app (Mission Planner, manual override/E-Stop, 3D digital twin, Plotly elevation surface), captured from a locally running build using a sample mission from the app's own demo generator. These are captioned as interface illustrations, not field measurements.

Three placeholders were left untouched because they call for things nobody can fabricate honestly:

- Figure 3.2: a photo or CAD render of the assembled chassis. Needs an actual photo of the physical build.
- Figure 4.1: the simulated path-tracking plot. Needs real output from a completed Gazebo trial.
- Figure 4.2: a photo of the field trial plot/rover. Needs an actual photo from a real trial.

Table 4.1 and 4.2 are also still TBD, same reason: no trial data exists yet.

When those are ready, drop the photo(s) in and swap them into the matching `[Placeholder: ...]` spot in the docx, or ask Claude to do it again.
